

networks:
  slms-net:
    driver: bridge

volumes:
  mongo-data:
  postgres-data:
  redis-data:
  geoip-data:
  ml-models:

services:
  # ── Infrastructure ─────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: slms-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    ports:
      - "6379:6379"
    networks:
      - slms-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo:
    image: mongo:7
    container_name: slms-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-slms}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-slmspassword}
      MONGO_INITDB_DATABASE: slms
    volumes:
      - mongo-data:/data/db
    networks:
      - slms-net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:16-alpine
    container_name: slms-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: slms
      POSTGRES_USER: ${POSTGRES_USER:-slms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-slmspassword}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - slms-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-slms}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Backend ────────────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: slms-backend
    restart: unless-stopped
    env_file: .env
    environment:
      REDIS_URL: redis://redis:6379
      MONGO_URL: mongodb://${MONGO_USER:-slms}:${MONGO_PASSWORD:-slmspassword}@mongo:27017/slms?authSource=admin
      POSTGRES_URL: postgresql+asyncpg://${POSTGRES_USER:-slms}:${POSTGRES_PASSWORD:-slmspassword}@postgres:5432/slms
      ML_MODEL_DIR: /app/ml_data
    volumes:
      - ml-models:/app/ml_data
      - geoip-data:/app/geoip
    ports:
      - "8001:8000"
    depends_on:
      redis:
        condition: service_healthy
      mongo:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - slms-net

  # ── Log Agents ─────────────────────────────────────────────────────────────
  agents:
    build:
      context: ./agents
      dockerfile: Dockerfile
    container_name: slms-agents
    restart: unless-stopped
    env_file: .env
    environment:
      REDIS_URL: redis://redis:6379
      BACKEND_URL: http://backend:8000
      HOST_OS_RELEASE: /host/etc/os-release
      PROCFS_PATH: /host/proc
      SYSFS_PATH: /host/sys
    volumes:
      - /var/log:/host/var/log:ro
      - /var/lib/docker/containers:/host/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # ── System telemetry mounts (read-only) ──────────────────────────────
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /etc/hostname:/host/etc/hostname:ro
    depends_on:
      redis:
        condition: service_healthy
    pid: host
    networks:
      - slms-net

  # ── Frontend ───────────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: slms-frontend
    restart: unless-stopped
    ports:
      - "3001:80"
    depends_on:
      - backend
    networks:
      - slms-net
