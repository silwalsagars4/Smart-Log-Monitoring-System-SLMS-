#! backend-ml-models-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: backend-ml-models
    namespace: slms
    labels:
        com.docker.compose.service: backend
        com.docker.compose.volume: ml-models
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
